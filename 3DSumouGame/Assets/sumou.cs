using UnityEngine;

public class sumou : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //みんなも魔法少女プリティベルを読もう！！

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
